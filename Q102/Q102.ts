// Question 102: Calculate and log the absolute difference between the number -5 and 5.

function calculateDifference(num1:number , num2:number) {
    return Math.abs(num1-num2);
}

console.log(calculateDifference(-5,5));
